fib:
    store[temp, 1] [param]
    pop [param]
    cmp [temp, 1], 2
    jwl return

    sub[param] [temp, 1], 2
    call[temp, 2] fib
    % pop [param]

    sub[param] [temp, 1], 1
    call[temp, 3] fib
    % pop [param]

    add[temp, 1] [temp, 2], [temp, 3]

    return:
        back [temp, 1]


main:
    push[param] 34
    %call[temp, 1] fib

    itos[param] 0%[temp, 1]
    sys 1
    pop [param]
    int 0